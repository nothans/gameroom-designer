/** Pixels per real-world inch on the canvas. Shared by App and RoomCanvas. */
export const PX_PER_INCH = 3;

/** Hard bounds for room dimensions (feet). */
export const MIN_ROOM_FT = 1;
export const MAX_ROOM_FT = 200;
